POSTGRES_USER=foyer
# Generate: openssl rand -hex 24
POSTGRES_PASSWORD=
POSTGRES_DB=foyer

# Generate: openssl rand -hex 32
JWT_ACCESS_SECRET=
JWT_ACCESS_TTL=30d

# Public domains served by your host reverse proxy (Apache/Caddy/nginx).
# Not consumed by compose; listed so the proxy vhost and the URLs below stay in sync.
APP_DOMAIN=app.gawula.co.za
API_DOMAIN=api.gawula.co.za

CORS_ORIGINS=https://app.gawula.co.za
APP_BASE_URL=https://app.gawula.co.za
NEXT_PUBLIC_API_URL=https://api.gawula.co.za/v1
NEXT_PUBLIC_SHOW_QA_CREDENTIALS=false
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

MAIL_HOST=
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=
MAIL_PASS=
MAIL_FROM="Gawula <no-reply@gawula.co.za>"

PAYMENTS_PROVIDER=stub
