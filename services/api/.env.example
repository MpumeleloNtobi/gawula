NODE_ENV=development
PORT=4000

DATABASE_URL=postgresql://foyer:foyer_dev@localhost:5433/foyer?schema=public
REDIS_URL=redis://localhost:6379

# Generate: openssl rand -hex 32
JWT_ACCESS_SECRET=replace_me_with_openssl_rand_hex_32
JWT_ACCESS_TTL=30d
# Generate: openssl rand -hex 32
JWT_REFRESH_SECRET=replace_me_with_openssl_rand_hex_32

CORS_ORIGINS=http://localhost:3000
APP_BASE_URL=http://localhost:3000
THROTTLE_TTL=60000
THROTTLE_LIMIT=120

PAYMENTS_PROVIDER=stub

# Mail (local dev uses Mailpit: from infra/ run `docker compose up -d mailpit`, inbox at http://localhost:8025)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_SECURE=false
MAIL_USER=
MAIL_PASS=
MAIL_FROM="Gawula <no-reply@gawula.co.za>"
